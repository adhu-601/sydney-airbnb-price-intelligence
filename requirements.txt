# Runtime dependencies for the deployed Streamlit app.
#
# Pinned to the exact, tested set the app was validated against (Python 3.14),
# so the hosted environment reproduces a known-good stack rather than resolving
# whatever is latest on each redeploy. This is the file Streamlit Community Cloud
# installs from.
#
# For local development and the full CLI/report toolchain (matplotlib, requests,
# pytest, ruff, …) install the project instead:  pip install -e ".[app,dev]"

streamlit==1.59.1
plotly==6.9.0
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
scipy==1.18.0
xgboost==3.3.0
pyarrow==25.0.0
joblib==1.5.3
PyYAML==6.0.3
