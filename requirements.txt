# Convenience mirror of pyproject.toml (install the project with
# `pip install -e ".[app,dev]"` for the full toolchain).
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
pyyaml>=6.0
joblib>=1.3
matplotlib>=3.8
requests>=2.31
pyarrow>=15.0
streamlit>=1.35
plotly>=5.20
pytest>=8.0
ruff>=0.4
